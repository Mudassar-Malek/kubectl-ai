# Core dependencies
click>=8.1.0
rich>=13.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
kubernetes>=28.0.0
requests>=2.31.0

# LLM Providers (choose one or both)
# Ollama - FREE local LLM (recommended for getting started)
# No package needed - uses REST API via requests

# Anthropic Claude - Paid API (optional)
anthropic>=0.39.0

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0

# Optional: Slack integration
# slack-bolt>=1.18.0
# slack-sdk>=3.21.0

# MCP (Model Context Protocol) server - for Cursor IDE integration
mcp>=1.0.0
